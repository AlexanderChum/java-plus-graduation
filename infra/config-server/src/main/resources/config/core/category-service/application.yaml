server:
  port: 0

spring:
  application:
    name: category-service
  datasource:
    driverClassName: org.postgresql.Driver
    username: category
    password: category
    url: jdbc:postgresql://localhost:6551/category
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: 'true'
    show_sql: 'true'
  sql:
    init:
      mode: always

eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname:localhost}:${eureka.instance.port:8761}/eureka/