server:
  port: 0

spring:
  application:
    name: compilation-service
  datasource:
    driverClassName: org.postgresql.Driver
    username: compilation
    password: compilation
    url: jdbc:postgresql://localhost:6555/compilation
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: 'true'
    show_sql: 'true'
  sql:
    init:
      mode: always

eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname:localhost}:${eureka.instance.port:8761}/eureka/