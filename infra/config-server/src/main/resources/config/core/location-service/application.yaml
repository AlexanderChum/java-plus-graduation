server:
  port: 0

spring:
  application:
    name: location-service
  datasource:
    driverClassName: org.postgresql.Driver
    username: location
    password: location
    url: jdbc:postgresql://localhost:6550/location
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: 'true'
    show_sql: 'true'
  sql:
    init:
      mode: always

eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname:localhost}:${eureka.instance.port:8761}/eureka/